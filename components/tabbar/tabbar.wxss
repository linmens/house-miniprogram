.t-tab-bar--placeholder {

  height: var(--td-tab-bar-height, 112rpx);
  box-sizing: border-box;
  user-select: none;
  padding: 0 24rpx;
  position: relative;
}

.t-tab-bar-safe {
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
  flex: 1;
}